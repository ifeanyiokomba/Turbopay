# TurboPay — Full Fix, Deploy & Push Plan

**Goal:** Fix all issues one-by-one in priority order, then deploy and push.
**Current state:** TypeScript 0 errors, ESLint 810 warnings, tests partially failing.

---

## Phase 1: Fix Test Infrastructure

### 1.1 Increase Prisma transaction timeout
The advisory lock tests fail because Prisma's interactive transaction default timeout (5s) is too short for concurrent operations.

**File:** `src/lib/turbopay/ledger.ts`
- Find `db.$transaction(async (tx) => {` calls
- Add `{ timeout: 15000 }` option to give concurrent operations enough time

### 1.2 Fix reconciliation test (CI-only failure)
MEMORY.md notes: `reconciliation.test.ts:52` — `wallet.update()` can't find record. Prisma test setup issue (wallet created in beforeAll not persisting to beforeEach).

**File:** `src/lib/turbocore/__tests__/reconciliation.test.ts`
- Check if the wallet is being deleted by a prior test's cleanup
- Add guard check in beforeEach (similar to advisory-lock.test.ts pattern)

### 1.3 Run full test suite to verify
- `npx vitest run` — all tests must pass

---

## Phase 2: Fix ESLint Warnings (810 → 0)

### 2.1 Remove unused imports (~100+ warnings)
Auto-fixable. Run `npx eslint . --fix` for the auto-fixable ones, then manually fix the rest.

Key files with most unused imports:
- Various view components importing unused UI components (CardTitle, CardHeader, Badge, etc.)
- Route handlers importing unused utilities (errorJson, audit, etc.)

### 2.2 Fix `no-mixed-spaces-and-tabs` (49 warnings)
All in one file. Run `npx eslint . --fix` — this is auto-fixable.

### 2.3 Fix `no-restricted-imports` (120 warnings)
Route handlers importing `@/lib/db` directly instead of using the service layer.

**Option A (pragmatic):** These are architectural warnings, not bugs. The eslint rule is already at "warn" level. For now, suppress with inline `eslint-disable` comments on the most critical routes, or accept them as tech debt.

**Option B (proper fix):** Refactor route handlers to use service layer. This is a large refactor (70+ routes).

**Recommended:** Accept as tech debt for now — these are warnings, not errors, and don't affect functionality.

### 2.4 Fix `@typescript-eslint/no-explicit-any` (~416 warnings)
The majority of warnings. These are `any` types in existing code.

**Recommended:** Accept as tech debt — these are warnings at "warn" level. Fixing 416 `any` types would be a massive refactor with high regression risk. The eslint config already has this at "warn" to avoid blocking the build.

### 2.5 Fix remaining warnings
- `@typescript-eslint/no-non-null-assertion` (21) — Accept as tech debt
- `react-hooks/exhaustive-deps` (8) — Fix the critical ones in payment flows
- Others (misc) — Fix case by case

### 2.6 Target: ESLint warnings < 100
After removing unused imports and fixing mixed tabs, we should be under 100 warnings (mostly `any` types and `no-restricted-imports` which are acceptable tech debt).

---

## Phase 3: Security Hardening

### 3.1 Create Next.js middleware (`src/middleware.ts`)
**Missing entirely.** Create middleware with:
- CSRF origin validation on state-changing requests
- Rate limiting (sliding window)
- Security headers (X-Content-Type-Options, X-Frame-Options, Referrer-Policy, X-XSS-Protection)

### 3.2 Fix devOtp leak in client components
MEMORY.md notes `devOtp` is leaked in response bodies across multiple client components.

**Files:** `src/components/turbopay/auth-screen.tsx`, `src/components/turbopay/views/settings.tsx`
- The client already handles `devOtp` for development convenience
- In production, the server should NOT return `devOtp` in responses
- Verify server routes don't return `devOtp` when `NODE_ENV=production`

### 3.3 Credential rotation
MEMORY.md notes credentials were exposed. These need rotation:
- Neon DB password
- Supabase DB password
- PII encryption key
- Provider API keys

**Note:** This requires the user to manually rotate credentials in each provider's dashboard. We can only prepare the .env files.

---

## Phase 4: Deploy & Push

### 4.1 Verify build
- `npx next build` — must complete successfully

### 4.2 Commit all changes
- `git add .`
- `git commit -m "fix: CSPRNG references, ESLint cleanup, test fixes, middleware"`

### 4.3 Push to GitHub
- `git push origin main`

### 4.4 Vercel auto-deploys
- Vercel watches the main branch and auto-deploys
- Verify deployment at https://turbopay.okomba.com

---

## Execution Order

| Step | Action | Verification |
|------|--------|-------------|
| 1 | Fix Prisma transaction timeout in ledger.ts | `npx vitest run` passes |
| 2 | Fix reconciliation test | `npx vitest run` passes |
| 3 | Remove unused imports (auto-fix) | `npx eslint .` warnings drop |
| 4 | Fix mixed spaces/tabs (auto-fix) | `npx eslint .` warnings drop |
| 5 | Fix exhaustive-deps warnings | `npx eslint .` warnings drop |
| 6 | Create Next.js middleware | Build succeeds |
| 7 | Verify devOtp not leaked in prod | Code review |
| 8 | Run full test suite | All tests pass |
| 9 | Run build | `npx next build` succeeds |
| 10 | Commit & push | Git status clean |
